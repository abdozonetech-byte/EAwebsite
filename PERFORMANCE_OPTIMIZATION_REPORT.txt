Elboubakry performance optimization report

Safe changes applied without changing UI/UX or animations:
- Removed Google Fonts @import waterfall from main.css.
- Added font preconnect + preload/stylesheet in HTML head for faster first paint.
- Added preconnect/dns-prefetch for Google Tag Manager and Meta Pixel domains.
- Deferred the external GA4 network script until browser idle/load while keeping gtag queue and page_view tracking.
- Added explicit dimensions and lazy/fetchpriority low to decorative hero shape images.
- Added mobile content-visibility for below-the-fold services/footer blocks to reduce rendering work.
- Kept all URLs, sitemap, schema, Pixel, Lead event, design, animations and content unchanged.

Recommended after deploy:
1. Clear Cloudflare cache.
2. Test homepage with PageSpeed Insights again.
3. Test /services/ and one service page on mobile.
