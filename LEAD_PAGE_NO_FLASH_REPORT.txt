Lead page no-flash fix

Changed file:
- reserver-diagnostic/index.html

What changed:
- Removed the visible static fallback diagnostic layout inside <div id="root">.
- Kept the React root container empty so the interactive lead page renders directly when the lead-page script loads.

Why:
- The fallback layout was visible for about one second after clicking reservation buttons, before the real lead page hydrated.
- Removing the fallback prevents users from seeing the temporary diagnostic screen.

Not changed:
- Lead page React bundle
- Lead form behavior
- Meta Pixel / Lead event logic
- GA4 analytics
- SEO metadata, schema, canonical URL
- Sitemap and redirects
- Homepage UI/UX
- Mobile/desktop animations
