UPGRADE 01 — Fix Leads / Diagnostic Form for Cloudflare

Scope:
- Fixed only the diagnostic/lead form production submission logic.
- Kept UI/UX unchanged.
- Kept mobile menu, hero animations, systems carousel unchanged.
- Kept SEO, Analytics, sitemap, robots, canonical and Open Graph intact.

Files changed:
1. assets/lead-page/reserver-diagnostic.js
   - Added a production-safe sendBeacon attempt before fetch.
   - Extended fetch timeout from 6.5s to 15s.
   - Added keepalive: true for production POST request.
   - Preserved the hidden iframe fallback.
   - The form only shows the error if all submission methods fail.

2. reserver-diagnostic/index.html
   - Added cache-busting query versions to diagnostic JS/CSS assets so Cloudflare/custom domain loads the new form logic.

Important test after deploy:
1. Open https://elboubakry.com/reserver-diagnostic/?v=upgrade1
2. Submit a test lead.
3. Confirm redirect to /merci/
4. Confirm lead appears in Google Sheets.
5. If old behavior remains, purge Cloudflare cache.
