/**
 * Central tracking configuration.
 *
 * IMPORTANT: the legacy archive supplied for this rebuild contained placeholders,
 * not real GA4 or Meta Pixel identifiers. Replace only the two values below with
 * the identifiers already used by your production accounts. Do not create a new
 * property or pixel unless that is intentional.
 */
window.ELBOUBAKRY_TRACKING = Object.freeze({
  GA4_MEASUREMENT_ID: 'G-KRP71BRZ5E',
  META_PIXEL_ID: '2322425664833523',
  ENABLE_GA4: true,
  ENABLE_META_PIXEL: true
});
